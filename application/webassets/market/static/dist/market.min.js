function setCurrentPageClass(){const nav_links=document.querySelectorAll("#menu>a");Array.from(nav_links).forEach(link=>{if(link.host==window.location.host&&link.pathname==window.location.pathname||window.location.pathname=='/'&&link.pathname=="/home"||window.location.pathname=='/index'&&link.pathname=="/home"){link.classList.add("currentPage");};});};setCurrentPageClass();function autocompleteMatch(input){if(input==''){return[];}
var reg=new RegExp(input.toUpperCase())
var names=ss
return names.filter(function(term){if(term.match(reg)){return term;}});}
function build_selections(terms){let selections=document.createElement("ul");for(i=0;i<terms.length;i++){let li=document.createElement("li");li.appendChild(document.createTextNode(terms[i]));li.setAttribute("onclick","update_input('"+terms[i]+"')");li.innertext=terms[i]
selections.appendChild(li);}
return selections}
function showResults(val){let res=document.getElementById("result");res.innerHTML='';if(ss.length>=5){let terms=autocompleteMatch(val);}
else{let terms=ss}
let options=build_selections(terms)
if(val.length>=2&&terms.length>1){res.innerHTML=options.innerHTML;}
else if(val.length==0&&ss.length<=5){}
else if(terms.length==1&&val==terms[0]){res.innerHTML="";}
else{res.innerHTML=options.innerHTML;;}}
function update_input(val){inp=document.getElementById("search");inp.value=val;showResults(val);}