function setCurrentPageClass(){const nav_links=document.querySelectorAll("#menu>a");Array.from(nav_links).forEach(link=>{if(link.host==window.location.host&&link.pathname==window.location.pathname||window.location.pathname=='/'&&link.pathname=="/home"||window.location.pathname=='/index'&&link.pathname=="/home"){link.classList.add("currentPage");};});};setCurrentPageClass();function solarnames_list(ss_json){solarnames=[]
for(let key in ss_json){solarnames.push(key)}
return solarnames}
function autocompleteMatch(input){if(input==''){return[];}
var reg=new RegExp(input.toUpperCase())
var system_names=solarnames_list(ss)
return system_names.filter(function(term){if(term.match(reg)){return term;}});}
function showResults(val){res=document.getElementById("result");res.innerHTML='';let list='';let terms=autocompleteMatch(val);for(i=0;i<terms.length;i++){list+='<li onclick=update_input("'+terms[i]+'")>'+terms[i]+'</li>';}
if(val.length>=2&&terms.length>1){res.innerHTML='<ul>'+list+'</ul>';}
else if(terms.length==1&&val==terms[0]){res.innerHTML=""}
else{res.innerHTML="";}}
function update_input(val){inp=document.getElementById("system_name");inp.value=val;showResults(val);}